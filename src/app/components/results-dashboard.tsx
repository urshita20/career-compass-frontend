import { Button } from "@/app/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/app/components/ui/card";
import { Badge } from "@/app/components/ui/badge";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/app/components/ui/tabs";
import { ArrowLeft, TrendingUp, Briefcase, BookOpen, Target, ExternalLink, Sparkles, CheckCircle2 } from "lucide-react";
import { CareerPath, learningResources, generateSkillGaps } from "./career-data";
import { motion } from "motion/react";

interface ResultsDashboardProps {
  recommendedCareers: CareerPath[];
  aiInsight: string | null; // NEW: AI insight prop
  onBack: () => void;
  onExploreMore: () => void;
  onStartLearning: (career: CareerPath) => void;
}

export function ResultsDashboard({ 
  recommendedCareers, 
  aiInsight, 
  onBack, 
  onExploreMore, 
  onStartLearning 
}: ResultsDashboardProps) {
  const topCareer = recommendedCareers[0];
  const skillGaps = generateSkillGaps(topCareer);
  const resources = learningResources[topCareer.id] || [];

  return (
    <div className="min-h-screen bg-gradient-to-br from-purple-100 via-pink-100 to-orange-100 dark:from-zinc-950 dark:via-zinc-900 dark:to-zinc-950 transition-colors duration-300">
      <div className="max-w-7xl mx-auto px-6 py-8">
        <Button onClick={onBack} variant="ghost" className="mb-6 dark:text-white dark:hover:bg-zinc-800">
          <ArrowLeft className="mr-2 h-4 w-4" />
          Back to Home
        </Button>

        {/* Header */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          className="text-center mb-10"
        >
          <h1 className="text-5xl md:text-6xl mb-4" style={{ fontWeight: 800 }}>
            Your Career Matches 🎯
          </h1>
          <p className="text-xl text-gray-600 dark:text-gray-300 max-w-2xl mx-auto">
            Based on your responses, we've found the perfect career paths for you!
          </p>
        </motion.div>

        {/* AI INSIGHT CARD - PROMINENT DISPLAY */}
        {aiInsight && (
          <motion.div
            initial={{ opacity: 0, scale: 0.95 }}
            animate={{ opacity: 1, scale: 1 }}
            transition={{ delay: 0.2 }}
            className="mb-8"
          >
            <Card className="border-2 border-purple-300 bg-gradient-to-br from-purple-50 via-pink-50 to-orange-50 dark:from-purple-900/30 dark:via-pink-900/30 dark:to-orange-900/30 dark:border-purple-700 shadow-xl">
              <CardHeader>
                <div className="flex items-center gap-3">
                  <div className="p-3 bg-gradient-to-br from-purple-500 to-pink-500 rounded-full">
                    <Sparkles className="h-6 w-6 text-white" />
                  </div>
                  <div>
                    <CardTitle className="text-2xl dark:text-white flex items-center gap-2">
                      AI Career Counselor Says
                      <Badge className="bg-gradient-to-r from-purple-600 to-pink-600 text-white">
                        Powered by Gemini
                      </Badge>
                    </CardTitle>
                    <CardDescription className="text-base dark:text-gray-300">
                      Personalized insight just for you ✨
                    </CardDescription>
                  </div>
                </div>
              </CardHeader>
              <CardContent>
                <div className="bg-white dark:bg-zinc-900 rounded-xl p-6 shadow-md">
                  <p className="text-lg leading-relaxed text-gray-800 dark:text-gray-200">
                    {aiInsight}
                  </p>
                </div>
                <div className="mt-4 flex items-center gap-2 text-sm text-gray-600 dark:text-gray-400">
                  <CheckCircle2 className="h-4 w-4 text-green-500" />
                  <span>Generated by AI based on your unique responses</span>
                </div>
              </CardContent>
            </Card>
          </motion.div>
        )}

        {/* Top Match - Hero Card */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.1 }}
          className="mb-8"
        >
          <Card className="border-2 border-purple-400 bg-gradient-to-br from-purple-600 to-pink-600 text-white shadow-2xl">
            <CardContent className="p-8">
              <div className="flex items-start justify-between mb-6">
                <div>
                  <Badge className="mb-3 bg-white text-purple-600 text-base px-4 py-1">
                    🔥 Top Match
                  </Badge>
                  <h2 className="text-4xl mb-2" style={{ fontWeight: 700 }}>
                    {topCareer.title}
                  </h2>
                  <p className="text-purple-100 text-lg">{topCareer.category}</p>
                </div>
                <div className="text-center">
                  <div className="text-6xl font-extrabold mb-2">
                    {topCareer.matchScore}%
                  </div>
                  <div className="text-sm text-purple-100">Match Score</div>
                </div>
              </div>

              <p className="text-lg mb-6 text-purple-50">{topCareer.description}</p>

              <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
                <div className="bg-white/10 backdrop-blur rounded-lg p-4">
                  <div className="flex items-center gap-2 mb-2">
                    <TrendingUp className="h-5 w-5" />
                    <span className="font-semibold">Growth Rate</span>
                  </div>
                  <div className="text-2xl font-bold">{topCareer.growthRate}</div>
                </div>
                <div className="bg-white/10 backdrop-blur rounded-lg p-4">
                  <div className="flex items-center gap-2 mb-2">
                    <Briefcase className="h-5 w-5" />
                    <span className="font-semibold">Average Salary</span>
                  </div>
                  <div className="text-2xl font-bold">{topCareer.averageSalary}</div>
                </div>
              </div>

              <div className="flex flex-col sm:flex-row gap-4">
                <Button
                  size="lg"
                  onClick={() => onStartLearning(topCareer)}
                  className="flex-1 bg-white text-purple-600 hover:bg-purple-50 text-lg h-14 rounded-full shadow-lg"
                >
                  Start Learning Path 🚀
                </Button>
                <Button
                  size="lg"
                  variant="outline"
                  className="flex-1 border-2 border-white text-white hover:bg-white/10 text-lg h-14 rounded-full"
                >
                  View Details
                </Button>
              </div>
            </CardContent>
          </Card>
        </motion.div>

        {/* Detailed Tabs for Top Career */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.2 }}
          className="mb-8"
        >
          <Tabs defaultValue="overview" className="w-full">
            <TabsList className="grid w-full grid-cols-3 mb-6">
              <TabsTrigger value="overview">Overview</TabsTrigger>
              <TabsTrigger value="skills">Skills & Path</TabsTrigger>
              <TabsTrigger value="pros-cons">Pros & Cons</TabsTrigger>
            </TabsList>

            <TabsContent value="overview">
              <Card className="dark:bg-zinc-900 dark:border-zinc-800">
                <CardHeader>
                  <CardTitle className="dark:text-white">Career Overview</CardTitle>
                </CardHeader>
                <CardContent className="space-y-6">
                  <div>
                    <h3 className="text-lg font-semibold mb-3 text-gray-700 dark:text-gray-300">
                      Required Skills
                    </h3>
                    <div className="flex flex-wrap gap-2">
                      {topCareer.requiredSkills.map((skill) => (
                        <Badge key={skill} variant="secondary" className="text-base px-3 py-1">
                          {skill}
                        </Badge>
                      ))}
                    </div>
                  </div>

                  <div>
                    <h3 className="text-lg font-semibold mb-2 text-gray-700 dark:text-gray-300">
                      Education Requirements
                    </h3>
                    <p className="text-gray-600 dark:text-gray-400">{topCareer.educationLevel}</p>
                  </div>

                  <div>
                    <h3 className="text-lg font-semibold mb-2 text-gray-700 dark:text-gray-300">
                      Work Environment
                    </h3>
                    <p className="text-gray-600 dark:text-gray-400">{topCareer.workEnvironment}</p>
                  </div>

                  <div>
                    <h3 className="text-lg font-semibold mb-2 text-gray-700 dark:text-gray-300">
                      Market Demand
                    </h3>
                    <Badge className={
                      topCareer.demandLevel === "High" ? "bg-green-500" :
                      topCareer.demandLevel === "Medium" ? "bg-yellow-500" :
                      "bg-blue-500"
                    }>
                      {topCareer.demandLevel} Demand
                    </Badge>
                  </div>
                </CardContent>
              </Card>
            </TabsContent>

            <TabsContent value="skills">
              <Card className="dark:bg-zinc-900 dark:border-zinc-800">
                <CardHeader>
                  <CardTitle className="dark:text-white">Skill Gap Analysis</CardTitle>
                  <CardDescription>Areas to focus on for this career</CardDescription>
                </CardHeader>
                <CardContent>
                  <div className="space-y-4">
                    {skillGaps.map((gap, index) => (
                      <div key={index} className="space-y-2">
                        <div className="flex justify-between items-center">
                          <span className="font-medium dark:text-white">{gap.skill}</span>
                          <Badge variant={gap.priority === "High" ? "destructive" : "secondary"}>
                            {gap.priority} Priority
                          </Badge>
                        </div>
                        <div className="flex items-center gap-4">
                          <div className="flex-1">
                            <div className="h-2 bg-gray-200 dark:bg-gray-700 rounded-full overflow-hidden">
                              <div
                                className="h-full bg-gradient-to-r from-purple-500 to-pink-500"
                                style={{ width: `${(gap.currentLevel / 10) * 100}%` }}
                              />
                            </div>
                          </div>
                          <span className="text-sm text-gray-600 dark:text-gray-400 w-24">
                            {gap.currentLevel}/10 → {gap.requiredLevel}/10
                          </span>
                        </div>
                        <p className="text-sm text-gray-600 dark:text-gray-400">
                          Estimated time: {gap.estimatedTime}
                        </p>
                      </div>
                    ))}
                  </div>
                </CardContent>
              </Card>
            </TabsContent>

            <TabsContent value="pros-cons">
              <div className="grid md:grid-cols-2 gap-6">
                <Card className="bg-green-50 dark:bg-green-900/20 border-green-200 dark:border-green-800">
                  <CardHeader>
                    <CardTitle className="text-green-700 dark:text-green-400">✅ Pros</CardTitle>
                  </CardHeader>
                  <CardContent>
                    <ul className="space-y-2">
                      {topCareer.pros.map((pro, index) => (
                        <li key={index} className="flex items-start gap-2 text-gray-700 dark:text-gray-300">
                          <span className="text-green-500 mt-1">•</span>
                          <span>{pro}</span>
                        </li>
                      ))}
                    </ul>
                  </CardContent>
                </Card>

                <Card className="bg-red-50 dark:bg-red-900/20 border-red-200 dark:border-red-800">
                  <CardHeader>
                    <CardTitle className="text-red-700 dark:text-red-400">⚠️ Cons</CardTitle>
                  </CardHeader>
                  <CardContent>
                    <ul className="space-y-2">
                      {topCareer.cons.map((con, index) => (
                        <li key={index} className="flex items-start gap-2 text-gray-700 dark:text-gray-300">
                          <span className="text-red-500 mt-1">•</span>
                          <span>{con}</span>
                        </li>
                      ))}
                    </ul>
                  </CardContent>
                </Card>
              </div>
            </TabsContent>
          </Tabs>
        </motion.div>

        {/* Other Recommendations */}
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.3 }}
        >
          <h3 className="text-2xl mb-4 dark:text-white" style={{ fontWeight: 700 }}>Other Great Matches</h3>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
            {recommendedCareers.slice(1).map((career, index) => (
              <motion.div
                key={career.id}
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: 0.1 * index }}
              >
                <Card className="border hover:border-purple-400 hover:shadow-xl transition-all cursor-pointer bg-white dark:bg-zinc-900 dark:border-zinc-800 group">
                  <CardContent className="p-6">
                    <div className="flex items-start justify-between mb-4">
                      <div>
                        <h3 className="text-xl mb-1 dark:text-white group-hover:text-purple-500 transition-colors" style={{ fontWeight: 600 }}>
                          {career.title}
                        </h3>
                        <p className="text-sm text-gray-600 dark:text-gray-400">{career.category}</p>
                      </div>
                      <div className="text-right">
                        <div className="text-3xl font-bold text-purple-600 dark:text-purple-400">
                          {career.matchScore}%
                        </div>
                        <div className="text-xs text-gray-500">Match</div>
                      </div>
                    </div>

                    <p className="text-sm text-gray-600 dark:text-gray-400 mb-4 line-clamp-2">
                      {career.description}
                    </p>

                    <div className="flex gap-3">
                      <Button
                        onClick={() => onStartLearning(career)}
                        className="w-full bg-gradient-to-r from-purple-600 to-pink-600 hover:from-purple-700 hover:to-pink-700 text-white"
                      >
                        Start Learning Path 🚀
                      </Button>
                    </div>
                  </CardContent>
                </Card>
              </motion.div>
            ))}
          </div>
        </motion.div>

        {/* CTA */}
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.4 }}
          className="mt-12 text-center"
        >
          <Card className="border-2 border-purple-200 bg-gradient-to-r from-purple-100 to-pink-100 dark:from-purple-900/40 dark:to-pink-900/40 dark:border-purple-800">
            <CardContent className="py-10">
              <h3 className="text-3xl mb-4 dark:text-white" style={{ fontWeight: 700 }}>
                Want to explore more careers?
              </h3>
              <p className="text-gray-700 dark:text-gray-300 mb-6 max-w-2xl mx-auto text-lg">
                Check out our full career library with detailed insights on 100+ career paths!
              </p>
              <Button
                onClick={onExploreMore}
                size="lg"
                className="bg-gradient-to-r from-purple-600 to-pink-600 hover:from-purple-700 hover:to-pink-700 text-white px-8 py-6 text-lg rounded-full shadow-lg"
              >
                <Target className="mr-2 h-5 w-5" />
                Explore All Careers
              </Button>
            </CardContent>
          </Card>
        </motion.div>
      </div>
    </div>
  );
}
